# Weekly cold-storage deep-clean rotation

The Master Sheet already schedules one cold-storage unit by monthly weekday position. The Worker must respect `BYDAY` and `BYSETPOS`, so only the scheduled unit appears for that week.
