name: Reverify canonical stale-aware OPS production

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/verify-stale-aware-production-v2.yml

permissions:
  contents: write

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Poll canonical OPS production
        shell: bash
        run: |
          origin='https://stupiaks-ops.sporkburger19.workers.dev'
          success=false
          response=''
          for attempt in $(seq 1 18); do
            response=$(curl -fsS --max-time 20 "$origin/api/health" || true)
            if grep -q 'stale-aware-v1' <<<"$response" && grep -q '120000' <<<"$response"; then
              success=true
              break
            fi
            sleep 10
          done
          mkdir -p /tmp/ops-verification
          {
            echo "canonical_origin=$origin"
            echo "stale_aware_verified=$success"
            echo "checked_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo "health_response=$response"
          } > /tmp/ops-verification/result.txt

      - name: Record result safely and remove verifiers
        shell: bash
        run: |
          git config user.name "stupiaks-ops-release"
          git config user.email "actions@users.noreply.github.com"
          git fetch origin main
          git reset --hard origin/main
          mkdir -p .deployments
          cp /tmp/ops-verification/result.txt .deployments/stale-aware-production-final-verification.txt
          git rm -f .github/workflows/verify-stale-aware-production-v2.yml
          git rm -f .github/workflows/verify-stale-aware-production-now.yml || true
          git add .deployments/stale-aware-production-final-verification.txt
          git commit -m "Record final stale-aware production verification"
          git push origin HEAD:main
