{
  "name": "chefops-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.dev.jsonc --port 8787",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test:data-package": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
